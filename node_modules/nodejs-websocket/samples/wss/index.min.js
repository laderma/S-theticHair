var fs=require("fs"),ws=require("../../"),options={secure:!0,key:fs.readFileSync("key.pem"),cert:fs.readFileSync("cert.pem")};ws.createServer(options,function(e){e.on("text",function(n){e.sendText(n.toUpperCase()+"!!!")})}).listen(8001,"127.0.0.1");