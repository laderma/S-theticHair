"use strict";function getNormalizedFile(n){return grunt.util.normalizelf(grunt.file.read(n))}var grunt=require("grunt"),comment=require("../tasks/lib/comment").init(grunt);exports.concat={default_options:function(n){n.expect(1);var e=getNormalizedFile("tmp/default_options"),t=getNormalizedFile("test/expected/default_options");n.equal(e,t,"should describe what the default behavior is."),n.done()},custom_options:function(n){n.expect(1);var e=getNormalizedFile("tmp/custom_options"),t=getNormalizedFile("test/expected/custom_options");n.equal(e,t,"should utilize custom banner, footer and separator."),n.done()},handling_invalid_files:function(n){n.expect(1);var e=getNormalizedFile("tmp/handling_invalid_files"),t=getNormalizedFile("test/expected/handling_invalid_files");n.equal(e,t,"will have warned, but should not fail."),n.done()},strip_banner:function(n){n.expect(7);var e=getNormalizedFile("test/fixtures/banner.js");n.equal(comment.stripBanner(e),grunt.util.normalizelf("// Comment\n\n/* Comment */\n"),"It should strip the top banner."),n.equal(comment.stripBanner(e,{block:!0}),grunt.util.normalizelf("// Comment\n\n/* Comment */\n"),"It should strip the top banner."),e=getNormalizedFile("test/fixtures/banner2.js"),n.equal(comment.stripBanner(e),grunt.util.normalizelf("\n/*! SAMPLE\n * BANNER */\n\n// Comment\n\n/* Comment */\n"),"It should not strip the top banner."),n.equal(comment.stripBanner(e,{block:!0}),grunt.util.normalizelf("// Comment\n\n/* Comment */\n"),"It should strip the top banner."),e=getNormalizedFile("test/fixtures/banner3.js"),n.equal(comment.stripBanner(e),grunt.util.normalizelf("\n// This is\n// A sample\n// Banner\n\n// But this is not\n\n/* And neither\n * is this\n */\n"),"It should not strip the top banner."),n.equal(comment.stripBanner(e,{block:!0}),grunt.util.normalizelf("\n// This is\n// A sample\n// Banner\n\n// But this is not\n\n/* And neither\n * is this\n */\n"),"It should not strip the top banner."),n.equal(comment.stripBanner(e,{line:!0}),grunt.util.normalizelf("// But this is not\n\n/* And neither\n * is this\n */\n"),"It should strip the top banner."),n.done()},process_function:function(n){n.expect(1);var e=getNormalizedFile("tmp/process_function"),t=getNormalizedFile("test/expected/process_function");n.equal(e,t,"should have processed file content."),n.done()}};